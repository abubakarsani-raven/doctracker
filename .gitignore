# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
*.log
e2e/.auth/
e2e/.env
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Production builds
dist/
build/
.next/
out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Prisma
# Migrations are deliberately NOT ignored. They are the source of truth for how
# the schema evolves, and `prisma migrate deploy` needs them present in the
# deployed image — without them it exits non-zero and the container never boots.

# Misc
*.tsbuildinfo
next-env.d.ts

# Uploaded files (local disk storage)
backend/uploads/
